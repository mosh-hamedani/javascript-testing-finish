{
  "name": "javascript-testing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "coverage": "vitest run --coverage",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.2.4",
    "vite": "^5.0.0",
    "vitest": "^1.1.3"
  },
  "dependencies": {
    "delay": "^6.0.0"
  }
}
